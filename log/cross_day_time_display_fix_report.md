# 跨日時段時間顯示修正報告

## 問題描述

用戶反映在計費明細中，跨日的夜間時段時間顯示不夠清楚。

### 原有問題
- 跨日時段顯示：`06-25~06-26 夜間平日 (18:25-07:25)`
- 用戶看不出這個時段是由多個收費週期合併而成
- 時間範圍顯示不夠詳細，無法清楚了解跨日的具體時間分段

### 用戶期望
- 希望看到分段的時間顯示，明確顯示跨日的時間分界點
- 例如：`06-25~06-26 夜間平日 (18:25-00:00, 00:00-07:25)`

## 解決方案

### 修正位置
- 文件：`app.py`
- 函數：`consolidate_billing_cycles`
- 行數：約第 679-695 行

### 修正邏輯
1. **檢測跨日且多週期合併的情況**
   - 當 `start_time.date() != end_time.date()` 且 `cycles_count > 1`
   - 且結束時間不是 00:00（避免整點結束的特殊情況）

2. **生成分段時間顯示**
   - 第一段：開始時間到午夜 (`18:25-00:00`)
   - 第二段：午夜到結束時間 (`00:00-07:25`)
   - 格式：`(18:25-00:00, 00:00-07:25)`

3. **保持向後兼容**
   - 單一週期或特殊情況仍使用原有邏輯
   - 同日時段不受影響

### 核心程式碼變更

```python
# 處理跨日顯示，格式如：06-20 日間平日 (14:22-22:22) 或 06-20~06-21 夜間假日 (18:25-00:00, 00:00-07:25)
if start_time.date() != end_time.date():
    # 跨日情況 - 檢查是否有多個收費週期合併，如果有則顯示分段時間
    if group["cycles_count"] > 1 and end_time.time() != time(0, 0):
        # 多個週期合併且不是整天到00:00結束，顯示分段時間
        # 顯示兩段時間：第一天到午夜，午夜到第二天結束時間
        first_part = f"{start_time.strftime('%H:%M')}-00:00"
        second_part = f"00:00-{end_time.strftime('%H:%M')}"
        time_range = f"({first_part}, {second_part})"
        period_display = f"{start_time.strftime('%m-%d')}~{end_time.strftime('%m-%d')} {segment_label} {time_range}"
    else:
        # 單一週期或整天到午夜，使用原有邏輯
        period_display = f"{start_time.strftime('%m-%d')}~{end_time.strftime('%m-%d')} {segment_label} ({start_time.strftime('%H:%M')}-{end_time.strftime('%H:%M')})"
```

## 測試驗證

### 測試案例 1：多天跨日停車
- **進場時間**：2025-06-25 07:25
- **出場時間**：2025-06-28 09:25
- **結果**：✅ 所有跨日夜間時段正確顯示分段時間

### 測試案例 2：簡單跨日停車
- **進場時間**：2025-06-25 22:30
- **出場時間**：2025-06-26 08:30
- **結果**：✅ 跨日夜間時段正確顯示為 `(22:30-00:00, 00:00-07:30)`

### 測試結果摘要
```
檢查跨日時段時間顯示:
✅ 跨日時段正確分段顯示: 06-25~06-26 夜間平日 (18:25-00:00, 00:00-07:25)
✅ 跨日時段正確分段顯示: 06-26~06-27 夜間平日 (18:25-00:00, 00:00-07:25)
✅ 跨日時段正確分段顯示: 06-27~06-28 夜間平日 (18:25-00:00, 00:00-07:25)
```

## 效果對比

### 修正前
```
06-25~06-26 夜間平日 (18:25-07:25)
780分鐘 (13小時) | 10元/60分鐘 = 100元
```

### 修正後
```
06-25~06-26 夜間平日 (18:25-00:00, 00:00-07:25)
780分鐘 (13小時) | 10元/60分鐘 = 100元
(合併了 13 個收費週期)
```

## 改進效果

1. **更清楚的時間範圍顯示**
   - 用戶可以清楚看到跨日的具體時間分段
   - 明確顯示午夜分界點

2. **更好的用戶體驗**
   - 計費明細更容易理解
   - 減少用戶對跨日計費的疑惑

3. **保持系統一致性**
   - 不影響其他時段的顯示
   - 向後兼容現有功能

## 影響範圍

### 直接影響
- 跨日時段的計費明細顯示
- 用戶界面的計費明細展示

### 無影響
- 計費邏輯和金額計算
- 非跨日時段的顯示
- API接口結構

## 總結

本次修正成功解決了用戶反映的跨日時段時間顯示問題：

✅ **問題解決**：跨日時段現在正確顯示分段時間  
✅ **用戶體驗提升**：計費明細更清楚易懂  
✅ **系統穩定性**：不影響現有功能和計費準確性  
✅ **測試通過**：多個測試案例驗證修正效果  

修正已完成並可投入使用。

---
*修正日期：2024-12-19*  
*修正人員：AI助手*  
*測試狀態：✅ 通過* 